# URL Shortener

A simple URL shortener built with Python and SQLite.

## What I Learned

- How URL shorteners work: hashing a long URL to a short code, storing the mapping in SQLite, and returning a 301 redirect from the short path
- Serving a minimal HTML frontend directly from a Python backend without a separate static file server